# Arrays

- A DS with similar elements
- int[] arr = new int[6]; The array will initially have garbage values defined.
- Instead of main(), if it's declared globally, the array will have 0 as the value initialized.
- Max size of array in main(String[] args) - 10^6
- Max size of array when defined globally - 10^7
- When an array is declared, a block of size 6 is created in computer's memory. 
- We can't predict the address location of 0th index of array block, but using indexes 
- we can relatively access all the blocks of the array